/*
module.exports = (app) => {
    const personalDetails = require('../controllers/personalDetails.controllers.js');
    
    //app.post('/personalDetails', personalDetails.create);

    app.get('/', personalDetails.root);

    app.get('/personalDetails', personalDetails.findAll);
    
    app.get('/personalDetails/:userId', personalDetails.findOne);

    app.put('/personalDetails/:userId', personalDetails.update);

    app.delete('/personalDetails/:userId', personalDetails.delete);
}
*/